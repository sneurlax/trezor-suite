diff --git a/node_modules/@evolu/common/dist/src/local-first/Shared.js b/node_modules/@evolu/common/dist/src/local-first/Shared.js
--- a/node_modules/@evolu/common/dist/src/local-first/Shared.js
+++ b/node_modules/@evolu/common/dist/src/local-first/Shared.js
@@ -118,6 +118,7 @@
             binaryType: "arraybuffer",
             onOpen: () => {
                 const ownerIds = transports.getClaimsForResource(transport);
+                globalThis.console.error("[EvoluSync] transportOpen url=" + transport.url + " ownerIds=" + [...ownerIds].join(","));
                 console.debug("transportOpen", {
                     url: transport.url,
                     ownerIds: [...ownerIds],
@@ -126,6 +127,12 @@
                     tenant.requestCreateSyncMessages(ownerIds);
                 });
             },
+            onClose: (event) => {
+                globalThis.console.error("[EvoluSync] transportClose url=" + transport.url + " code=" + event?.code + " reason=" + event?.reason + " wasClean=" + event?.wasClean);
+            },
+            onError: (error) => {
+                globalThis.console.error("[EvoluSync] transportError url=" + transport.url + " error=" + String(error));
+            },
             onMessage(data) {
                 if (!(data instanceof ArrayBuffer))
                     return;
@@ -399,6 +406,7 @@
                     const webSocket = deps.transports.getResource(transport);
                     if (!webSocket?.isOpen())
                         continue;
+                    globalThis.console.error("[EvoluSync] sendProtocolMessage ownerId=" + ownerId + " url=" + transport.url + " bytes=" + protocolMessage.byteLength);
                     console.debug("sendProtocolMessage", {
                         ownerId,
                         url: transport.url,
@@ -422,7 +430,10 @@
         const toggleSyncOwner = (instance, syncOwner, action) => async (run) => {
             if (action === "add") {
                 instance.usedSyncOwners.increment(syncOwner);
-                return await run(deps.transports.addClaim(syncOwner.owner.id, syncOwner.transports));
+                globalThis.console.error("[EvoluSync] toggleSyncOwner BEFORE_ADDCLAIM ownerId=" + syncOwner.owner.id);
+                const claimResult = await run(deps.transports.addClaim(syncOwner.owner.id, syncOwner.transports));
+                globalThis.console.error("[EvoluSync] toggleSyncOwner AFTER_ADDCLAIM ownerId=" + syncOwner.owner.id + " ok=" + claimResult?.ok + " err=" + JSON.stringify(claimResult?.error));
+                return claimResult;
             }
             else {
                 instance.usedSyncOwners.decrement(syncOwner);
@@ -490,6 +501,7 @@
                         case "UseOwner": {
                             void tenantRun(instance.useOwnerMutex.withLock(async (run) => {
                                 for (const { owner, action } of message.actions) {
+                                    globalThis.console.error("[EvoluSync] useOwner action=" + action + " ownerId=" + owner.owner.id + " urls=" + owner.transports.map(({ url }) => url).join(","));
                                     console.debug("useOwner", {
                                         id: instance.id,
                                         action,
diff --git a/node_modules/@evolu/common/dist/src/Resource.js b/node_modules/@evolu/common/dist/src/Resource.js
--- a/node_modules/@evolu/common/dist/src/Resource.js
+++ b/node_modules/@evolu/common/dist/src/Resource.js
@@ -147,6 +147,7 @@
                     idleDisposeFiber = undefined;
                 }
                 if (!current) {
+                    globalThis.console.error("[EvoluSync] sharedResource ACQUIRE_BEFORE_CREATE");
                     const resource = await run(create);
                     if (!resource.ok)
                         return resource;
@@ -342,6 +343,7 @@
             const disposables = disposer.move();
             return ok({
                 addClaim: (claim, resourceKeys) => unabortable(() => sharedResourceClaimsRun(async (run) => {
+                    globalThis.console.error("[EvoluSync] addClaim TASK_START claim=" + claim + " keys=" + resourceKeys.length);
                     assertNoDuplicateResourceKeys(resourceKeys);
                     for (const resourceKey of resourceKeys) {
                         const added = await run(mutexByKey.withLock(resourceKey, async (run) => {
@@ -357,6 +359,7 @@
                                 return ok();
                             }
                             const hasClaimsForKey = keyByClaim.hasB(resourceKey);
+                            globalThis.console.error("[EvoluSync] addClaim MUTEX_BEFORE_ACQUIRE hasClaimsForKey=" + hasClaimsForKey + " url=" + (resourceKey?.url ?? String(resourceKey)));
                             let firstResource;
                             if (!hasClaimsForKey) {
                                 const resourceResult = await run(sharedResourcesByKey.acquire(resourceKey));
diff --git a/node_modules/@evolu/common/dist/src/WebSocket.js b/node_modules/@evolu/common/dist/src/WebSocket.js
--- a/node_modules/@evolu/common/dist/src/WebSocket.js
+++ b/node_modules/@evolu/common/dist/src/WebSocket.js
@@ -63,6 +63,7 @@
 import { ArrayBuffer, String, Uint8Array } from "./Type.js";
 /** Create a new {@link WebSocket}. */
 export const createWebSocket = (url, { protocols, binaryType, onOpen, onClose, shouldRetryOnClose = lazyTrue, onMessage, onError, schedule = jitter(1)(maxDelay("30s")(exponential("100ms"))), WebSocketConstructor = globalThis.WebSocket, } = {}) => async (run) => {
+    globalThis.console.error("[EvoluSync] createWebSocket TASK url=" + url);
     const env_1 = { stack: [], error: void 0, hasError: false };
     try {
         const disposer = __addDisposableResource(env_1, new AsyncDisposableStack(), true);
@@ -86,6 +87,7 @@
          */
         const connect = callback(({ err, ok }) => {
             closeSocket();
+            globalThis.console.error("[EvoluSync] createWebSocket CONNECT url=" + url + " constructor=" + (WebSocketConstructor?.name ?? String(WebSocketConstructor)));
             socket = new WebSocketConstructor(url, String.is(protocols) ? protocols : protocols && [...protocols]);
             if (binaryType)
                 socket.binaryType = binaryType;
@@ -107,6 +109,7 @@
                 onMessage?.(event.data);
             };
             socket.onerror = (event) => {
+                globalThis.console.error("[EvoluSync] createWebSocket SOCKET_ERROR url=" + url + " isOpen=" + isOpen + " event=" + String(event?.message ?? event));
                 const error = isOpen
                     ? { type: "WebSocketConnectionError", event }
                     : { type: "WebSocketConnectError", event };
